import React from 'react'
import {ActivityIndicator} from 'react-native'

const Spinner = () => (
      <ActivityIndicator
        size={60}
        color={"red"}
        style={{marginTop: '30%'}}
      />
)

export default Spinner